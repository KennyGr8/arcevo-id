enum Role {
  USER
  PREMIUM
  PRO
  ADMIN
}

enum UserStatus {
  active
  suspended
  banned
}

enum AdminAction {
  USER_BAN
  USER_UNBAN
  ACCOUNT_DELETED
}

enum TokenType {
  VERIFY_EMAIL
  RESET_PASSWORD
  MAGIC_LINK
  TOTP
}

enum ActivityType {
  LOGIN
  LOGOUT
  MFA_ENABLED
  PASSWORD_RESET
}

enum AuthEvent {
  LOGIN
  LOGOUT
  MFA_SUCCESS
  MFA_FAILED
}

enum SubscriptionPlan {
  FREE
  PREMIUM
  PRO
  ENTERPRISE
}

enum SubscriptionStatus {
  ACTIVE
  CANCELED
  TRIALING
  EXPIRED
}

enum SubscriptionProvider {
  STRIPE
  PAYSTACK
  LEMONSQUEEZY
}

enum OAuthProvider {
  GITHUB
  GOOGLE
  DISCORD
}
